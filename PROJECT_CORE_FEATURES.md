# 프로젝트 핵심 기능 분석 (PROJECT_CORE_FEATURES.md)

## 1. 프로젝트 개요

이 문서는 해당 프로젝트의 전체적인 구조와 핵심 기능을 분석하여 유사한 프로젝트를 리버스 엔지니어링하는 데 도움을 주기 위해 작성되었습니다.

이 프로젝트는 다수의 하위 모듈 및 애플리케이션으로 구성된 모노레포 구조를 가지고 있으며, 주로 TypeScript로 작성된 것으로 보입니다. PNPM 워크스페이스와 Turborepo를 사용하여 패키지 관리 및 빌드 시스템을 효율적으로 운영하고 있습니다.

핵심 기능은 멀티모달 AI 에이전트 개발 및 관련 UI 애플리케이션 제공에 초점이 맞춰져 있는 것으로 추정됩니다.

## 2. 주요 폴더 구조 및 역할

*   **`.github/`**: GitHub Actions 워크플로우, 이슈 템플릿 등 GitHub 연동 및 CI/CD 관련 설정이 포함됩니다.
*   **`apps/`**: 실제 실행 가능한 애플리케이션들이 위치합니다.
    *   `apps/ui-tars/`: Electron 기반의 UI 애플리케이션으로 추정되며, 프로젝트의 핵심 기능을 사용자 친화적인 인터페이스로 제공하는 역할을 할 것으로 보입니다.
*   **`docs/`**: 프로젝트 관련 문서 (사용자 가이드, API 문서 등)를 포함합니다.
*   **`examples/`**: 프로젝트의 다양한 기능 및 모듈 사용 예시 코드를 제공합니다.
*   **`multimodal/`**: 핵심 AI 기능 및 에이전트 관련 모듈들이 위치합니다. "멀티모달"이라는 이름에서 알 수 있듯이 텍스트, 이미지 등 다양한 종류의 데이터를 처리하는 기능을 포함할 가능성이 높습니다.
    *   `multimodal/agent/`: 기본적인 에이전트 로직 및 프레임워크를 포함할 수 있습니다.
    *   `multimodal/agent-tars/`: "TARS"라는 특정 에이전트 또는 시스템과 관련된 코드입니다. 이 프로젝트의 핵심 에이전트일 가능성이 있습니다.
    *   `multimodal/codeact-agent/`: 코드 실행과 관련된 에이전트 기능일 수 있습니다.
    *   `multimodal/deep-research-agent/`: 심층적인 정보 검색 및 분석 기능을 수행하는 에이전트일 수 있습니다.
    *   `multimodal/llm/`, `multimodal/llm-client/`: 대규모 언어 모델(LLM) 연동, API 클라이언트 등의 기능을 제공합니다.
    *   `multimodal/model-provider/`: 다양한 AI 모델을 통합하고 관리하는 인터페이스를 제공할 수 있습니다.
    *   `multimodal/agent-tars-cli/`: TARS 에이전트를 위한 CLI (Command Line Interface) 도구일 수 있습니다.
    *   `multimodal/agent-tars-server/`: TARS 에이전트 기능을 제공하는 서버 애플리케이션일 수 있습니다.
*   **`packages/`**: 재사용 가능한 라이브러리 및 모듈들이 위치합니다. `apps/` 및 `multimodal/`의 여러 부분에서 공통적으로 사용될 수 있습니다.
    *   `packages/agent-infra/`: 에이전트 시스템의 기반이 되는 인프라 (브라우저 자동화, 로깅, 검색 API 연동 등)를 제공합니다.
    *   `packages/ui-tars/`: `apps/ui-tars/` 애플리케이션에서 사용되는 UI 컴포넌트, SDK, 관련 로직 등을 포함할 수 있습니다.
*   **`scripts/`**: 프로젝트 빌드, 테스트, 배포 등 개발 프로세스 자동화를 위한 스크립트 파일들이 위치합니다.

## 3. 주요 기술 스택 및 도구 (추정)

*   **언어**: TypeScript
*   **패키지 매니저**: PNPM (모노레포 관리)
*   **빌드 시스템**: Turborepo (모노레포 빌드 최적화), Electron (데스크톱 애플리케이션 개발, `apps/ui-tars/`)
*   **테스팅**: Vitest (파일 구조에서 확인됨)
*   **AI/LLM**: 자체 개발 또는 외부 LLM API (OpenAI, Anthropic 등) 연동
*   **UI 프레임워크**: `apps/ui-tars/` 내부에서 React, Vue, Svelte 등의 프레임워크 사용 가능성 (추후 세부 분석 필요)

## 4. 모듈 간 의존성 관계 (추정)

*   **모노레포 구조**: `pnpm-workspace.yaml` 파일을 통해 `packages/` 내부의 모듈들이 `apps/` 또는 `multimodal/` 내부의 다른 모듈/애플리케이션에서 의존성으로 사용될 것입니다.
*   **UI와 에이전트 연동**: `apps/ui-tars/` 애플리케이션은 `multimodal/agent-tars/` 또는 다른 에이전트 모듈의 기능을 호출하여 사용자 인터페이스를 통해 제공할 가능성이 높습니다. `packages/ui-tars/sdk`가 이 연동을 위한 SDK 역할을 할 수 있습니다.
*   **에이전트와 LLM**: `multimodal/` 내의 에이전트들은 `multimodal/llm-client/`를 통해 LLM 기능을 활용하고, `multimodal/model-provider/`를 통해 다양한 모델을 선택적으로 사용할 수 있을 것입니다.
*   **에이전트와 인프라**: 모든 에이전트들은 `packages/agent-infra/`의 공통 인프라(브라우저 제어, 검색 기능 등)를 활용할 것으로 예상됩니다.

## 5. 핵심 기능 추론

*   **멀티모달 AI 에이전트**: 텍스트, 이미지 등 다양한 입력을 이해하고 작업을 수행할 수 있는 AI 에이전트 개발이 핵심입니다.
*   **웹 브라우저 자동화**: 에이전트가 웹 브라우저를 제어하여 정보 수집, 웹사이트와 상호작용 등의 작업을 수행할 수 있습니다. (`packages/agent-infra/browser/`)
*   **LLM 기반 작업 처리**: LLM을 활용하여 자연어 이해, 생성, 추론 등의 작업을 수행합니다.
*   **데스크톱 UI 애플리케이션**: 사용자가 AI 에이전트와 쉽게 상호작용하고 작업을 관리할 수 있는 GUI를 제공합니다. (`apps/ui-tars/`)
*   **확장 가능한 에이전트 및 도구 시스템**: 다양한 종류의 에이전트(TARS, CodeAct, DeepResearch 등)와 도구(Tool)를 개발하고 통합할 수 있는 구조를 가지고 있을 것입니다.
*   **CLI 및 서버 제공**: UI뿐만 아니라 CLI를 통한 에이전트 제어, 서버를 통한 API 제공 등 다양한 방식으로 에이전트 기능을 활용할 수 있도록 지원할 가능성이 있습니다.

## 6. 향후 분석 방향

*   각 `package.json` 파일을 분석하여 정확한 외부 라이브러리 의존성을 파악합니다.
*   `multimodal/` 및 `packages/` 내부의 주요 모듈들의 소스 코드를 분석하여 세부 기능을 파악합니다.
*   `apps/ui-tars/`의 UI 구현 방식과 상태 관리 로직을 분석합니다.
*   에이전트들의 프롬프트 엔지니어링 방식 및 LLM과의 상호작용 방식을 분석합니다.

이 문서는 초기 분석을 바탕으로 작성되었으며, 향후 세부 분석을 통해 내용이 업데이트될 수 있습니다.
